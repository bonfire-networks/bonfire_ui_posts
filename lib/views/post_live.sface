<StatelessComponent
  module={maybe_component(Bonfire.UI.Social.ObjectThreadLive)}
  myself={nil}
  thread_title={@thread_title}
  thread_mode={@thread_mode}
  activity={@activity}
  showing_within={:thread}
  object={@object}
  post_id={@post_id}
  thread_id={e(@thread_id, nil)}
  participants={@participants}
/>
